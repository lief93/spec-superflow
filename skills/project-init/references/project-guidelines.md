# 项目开发基线

> 确认状态：`草稿`
>
> 确认人：`<developer>`
>
> 最后检查时间：`<date>`

本文让开发人员或 AI 拿到普通需求后，能够定位代码位置并沿项目既有链路完成实现。具体业务字段和接口参数由本次需求 Spec 定义。

## 技术与框架约束

| 实现问题 | 项目统一机制 | 开发时怎么做 | 适用范围 | 参考 |
|---|---|---|---|---|
| `<implementation concern>` | `<approved mechanism>` | `<concrete integration method>` | `<scope>` | `<path#symbol>` |

## 架构与编码规则

| 要实现的改动 | 由谁负责 | 应该如何协作 | 不应该出现 | 参考 |
|---|---|---|---|---|
| `<common change>` | `<owning role>` | `<allowed data, event, and dependency flow>` | `<wrong owner, bypass, or duplicate>` | `<evidence>` |

## 经典实现索引

| 开发场景 | 首选参考 | 复用的核心模式 |
|---|---|---|
| `<common requirement>` | `<path#symbol>` | `<end-to-end implementation pattern>` |

### `<requirement recipe name>`

- 适用条件：`<when this recipe applies>`
- 新建或修改：`<specific roles and files>`
- 实现顺序：
  1. `<first concrete step>`
  2. `<second concrete step>`
  3. `<third concrete step>`
- 必须保持：`<architecture invariant and source of truth>`
- 完成标准：`<observable result and verification>`
- 参考实现：`<path#symbol>`
