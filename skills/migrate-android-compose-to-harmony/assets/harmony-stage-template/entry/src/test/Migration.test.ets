import { describe, expect, it } from '@ohos/hypium';

export default function migrationTemplateTests() {
  describe('MigrationTemplate', () => {
    it('runsUnitTestGate', 0, () => {
      expect(true).assertTrue();
    });
  });
}
