# Verification status

2026-09-13, independent native first slice. Work confined to verification/ and
new /tmp runs. No generator/core/fixture/old Skill/prototype/evidence modification.

## Final Run11 Closeout: User-Directed Stop

This section supersedes the historical next-step lists below. Page verification
is stopped; no new target captures, MaterialText comparisons or probes are queued.
Run `/private/tmp/kotlin-ets-native-20260913-11/CLOSEOUT.md` records completed gates
and omissions. Android SDK and both Harmony SDK builds/installations PASS;
Hypium1/1 PASS with failures0/errors0/ignore0. Android seven settled states PASS,
all settled MAE0. Five guarded boundary taps yield page2/2/1/2/2 for padding-ring,
painted-inner, gap-left, gap-tie, gap-right. Harmony paired capture/comparison was
not started. Run10 geometry/touch failures are retained, not declared repaired.

Frozen tool root: `/private/tmp/kotlin-ets-frozen-eff36-20260913-01/tools/kotlin-ets`.
Frozen source manifest: `/private/tmp/kotlin-ets-frozen-eff36-20260913-01/manifest.json`.
Manifest SHA256: `dd7fc4a4c2f0f08ff183839417c2d830d329c978150d494a689f0f58e0fda4d1`.
293 read-only files; production input inventory matched run11 before/after copy.
`run11/frozen-inputs.json` binds the isolated baseline; live architecture work
must not be mixed into it. Cached compiler/JDK dependencies remain external.

Both authorized device verification applications were force-stopped and no PID
remained; exact commands are in `run11/session-end/commands.json`. No further
device work is planned. Evidence index: `evidence/run11-closeout-manifest.json`.
This is diagnostic closure, not full acceptance. No production repair was made.

## Actual source Android

Run `/private/tmp/kotlin-ets-native-20260913-04` compiled actual `sample.Page()`
with Kotlin/Compose compiler 2.1.20 and Android SDK 35. Installed only on
`emulator-5560`; APK pulled back and hash matched:
`804811a977e299e987ca6985f772f3f3be9fbb686c94f5c62cdc5a82f3e4f1f0`.

`capture-android-4/result.json`: seven states pass, retained raw tree and two
settled full screenshots per state. Actual environment en-US/fontScale1/light,
density3.5 and content [0,0,1320,2856]. Source dimensions: computed spacing56px,
pager840px, button168px; four exact70x28px black/gray136 indicator paints with
98px step. Swipe0->1->2->3, back2 restores Continue, click yields Callback1 and
page3/Finish, restart0/Callback0. Padding ring [301,1164] leaves page0; painted
inner [343,1164] navigates to page2. No AI image inspection.

Earlier run03 viewport failure retained (Android cutout-safe height2724).
Run04 first attempt started before installer finished and failed; no pass claim.
Run04 second attempt exposed expanded accessibility touch rectangles; numerical
paint detection replaced the invalid semantic-center sample. All failures remain.

## Generated ETS SDK

Run04 generated-2 successful normal CLI; real SDK rejected missing __etsIntRem
and @Entry root builder-only call (see build-harmony/001-harmony-default.stderr).

Run05 generated-1 includes the repaired core prelude. Generation detected core
file drift and therefore is diagnostic-only. Real SDK no longer reports missing
__etsIntRem; still rejects @Entry.build() calling this.Page() as its root:
`10905210`, Index.ets:168. No generated bytes were edited. No new Harmony HAP
has been installed or claimed accepted at this point.

Exact reusable newline classpath (actual Gradle compile libraries):
`/private/tmp/kotlin-ets-native-20260913-04/compose-classpath.txt`.

## Frozen Run08 Actual Target Findings

Run `/private/tmp/kotlin-ets-native-20260913-08` uses the production freeze with
UI SHA `242f0203190be7cfcf6bd0dcdbe35cc45c711a6903ddc70670866e57cf1ad6e9`.
Fresh normal CLI input/output and compiler hashes are in `generated-1/`.
Android and both Harmony SDK builds PASS. Fresh main-then-test HAP install PASS.
HAP readback from `/data/app` is OS permission denied; `install-harmony/readback.json`
states that limit. Exact local payload hashes and install receipts are retained,
and the current application PID logs the expected generated ETS SHA.

- APK: `804811a977e299e987ca6985f772f3f3be9fbb686c94f5c62cdc5a82f3e4f1f0`.
- Main HAP: `09b2652ee049b4e670bec6d73bdda84fca4d2c05253615c8bd2b3902ab14e4ca`.
- Test HAP: `efdc9f6428ce6a5b92ac12836b1c4936a1052b383340d0be18de6057e11c4153`.

`hypium-1/report.json`: actual1/1 pass, Failure0/Error0/Ignore0,13648ms. This
test checked source strings, page state, backward navigation and callback; it
did not establish non-overlapping layout. The next test template additionally
asserts source pager/button/indicator dimensions and ordering.

`capture-android-1/result.json`: seven states and boundary probes PASS again.
`capture-harmony-1/result.json`: initial layout FAIL. Pager is correct at
[70,266,1250,1106], but indicator0 is [112,308,182,336], page/callback text top266,
and action button [70,266,1250,434]. Android action button top1388. Successive
slot roots overlap rather than flowing in the source Column. Raw tree and both
settled PNGs are retained; no generated output has been patched.

`initial-diagnostic/report.json`: full-frame MAE15.04328571 (limit8), changed
pixel fraction0.11113578 (limit0.08), button position delta1122px (limit3.5px).
This is an explicit failed initial pair, not full seven-state visual acceptance.
Manager routed the defect to the same UI worker; fresh generation is held until
that worker freezes its output-quality and slot-layout repairs.

## ModifierOrder Source Oracle

Run `/private/tmp/kotlin-ets-modifiers-20260913-01`: actual Compose instrument
test1/1 PASS. Source bounds outer[0,0,98,56], inner[14,70,84,98] at density3.5.
Physical ADB confirms the same sequence as Compose instrumentation: initialHits0,
outer-ring(7,28)->Hits1, inner-ring(7,84)->Hits2, inner-painted(49,84)->Hits3.
The isolated inner target expands natively; do not infer ring exclusion from
its nominal20x8dp layout bounds. In the main fixture's adjacent indicator row,
earlier unguarded attempts sometimes recorded a no-op. This is superseded by
the guarded run10 source oracle below; do not generalize ring exclusion.

## Remaining

The UI worker's manually authored WrappedBuilder syntax/layout diagnostic is
verified separately in `/private/tmp/kotlin-ets-slot-probe-20260913-01`.
Both signed HAP SDK builds and fresh main-then-test installation PASS;
`preservesCallerColumnAndRow` Hypium1/1 PASS, failures0/errors0/ignore0.
`capture-harmony-slot-probe-1/result.json` retains exact native bounds: Column
children140x70px at y0/70/140/210, Row children140x70px at x0/140/280/420 and
common y280. Raw tree and screenshot hashes retained without AI image viewing.
This proves the proposed slot primitive preserves both caller axes, but is NOT
CLI-generated translation acceptance. Production repair/freeze remains required.

- Fresh generation after the current UI repair/freeze, stable implementation hash gate.
- Repeat main/test HAP builds and fresh install with strengthened layout Hypium.
- Harmony equivalent states, geometry, callback/restart and padding click probes.
- Paired numerical visual comparison with fixed documented thresholds.
- ModifierOrder corresponding generated target geometry/physical taps.
- Bounded SourceValues actual SDK/runtime values after mandatory Page/ModifierOrder.

This is a handoff checkpoint, not final acceptance or review. No commit/push or
cache synchronization occurred.

## Run09 Android Startup Failure

`/private/tmp/kotlin-ets-native-20260913-09` rebuilds the unchanged source APK
with SHA804811a9... and retains seven successful state observations, but the
full Android capture FAILS on its second boundary restart. Android reports
`am_process_start_timeout` for dev.ets.verification PID21633 at device16:41:49,
then kills it and resumes the historical Contact activity. The launch command
misleadingly returned Status:ok with LaunchState UNKNOWN and WaitTime20229.
Read-only event evidence is in `android-foreground-diagnostic/`; there is no
proof of another device actor. An initial race suspicion was corrected after
these logs. This attempt is not a valid complete paired oracle; fresh retry
is required after local build load settles. No historical app was modified.

## Run10 Repaired Slots, Remaining Parity Failures

Production UI SHA847c0af5ebfa5f388e75b8d1cbb03d172cf79bc9ff8b4f5df4e1b062f9ceabb8;
generated ETS SHAf6949db841279bc6639dd43da5ad3c244b5afcee43c7f2ffe93fabdfb920dded.
Fresh normal CLI, both SDK HAP builds, main-then-test install, and strengthened
Hypium1/1 PASS failures0/errors0/ignore0. Main HAP
ba104aaa0abdd78026e8d8a18390b6a60e553252a9962db4ac2cd68dc905c30a;
test HAP f390a9bfd59fcea8902be14a7d2ad3b1df7dd238fa03db51d5fc4b6f2702cc43.

Android attempt1 rejected a stale Banking App ANR dialog before taps or images.
Recorded cleanup force-stopped that stale process, without changing its APK or
historical evidence. `capture-android-2` and `capture-harmony-1` both PASS seven
states including restart, source56px spacing, indicator paints70x28px and outer
98x56px. Guards verify foreground/package and initial Page0/Callback0 before
each boundary tap. No local SDK build ran during these captures.

`comparison/comparison.json` FAIL: all seven full-frame pixel comparisons pass
MAE7.1974..7.4897 <=8 and changed fraction0.071694..0.073021 <=0.08, but button
geometry delta38px exceeds3.5px. Source button top1388 vs target1350; default
Page/Callback Text heights84px source vs66px target. Pager/indicators differ2px.
No coordinate correction or threshold widening was applied.

Same absolute padding-ring tap(301,1164): guarded Android->page2, Harmony->page0.
Painted-inner(343,1164)->page2 on both. Run08 raw Android boundaries also record
ring->page2; earlier narrative excluding the ring was not a reliable source
oracle. Actual Compose minimum touch expansion must be preserved. UI/manager
received exact bounds, trees, and touch evidence. Both issues remain RED.

Run10 is diagnostic-only: UI production is changing again for the required
pure @Composable value-return helper classification plus parity repairs. A
new frozen generation/build/device identity gate remains required.
