// Explicit test-host implementations of the two stateless external receiver types.
// Inline method bodies are translated from the real binary IR, not supplied here.
export class FinalMember {}
export class PeerMember {}
