# Frozen Architecture Verification

2026-09-13 final run: GREEN. This supersedes the pending-freeze note in historical
RESULTS.md. Earlier documents and tested files were left unchanged so their
recorded input hashes remain checkable. Only this final result document was
added after the run. No production edits, page tests or device operations.

## Backend And Host Contracts

Command: `bash tools/kotlin-ets/tests/backend/run.sh`

Evidence:
`/var/folders/fj/rrz0bjhx6cq04j7yghy2qkxh0000gn/T/kotlin-ets-backend-tests.IAWxTM`

Both actual official-IR fixtures PASS all typed AST, source method/parameter,
default argument, conditional/while, object, lexical identity and closure checks.
Actual resolved source function calls are nonempty and every tested call has
external=false with its symbol equal to the corresponding EtsFunction.symbol.
Earlier lexical symbol identity assertions remain intact. Malformed CallRule
results fail during lower with the exact source call span and no EtsProgram.

Lowering compiles and executes without Printer available. Separately compiled
Printer passes repeated identical-AST determinism and non-mutation checks.
JVM/printed-target host differential passes all ten values. Input hash stability
check exits0. Input manifest SHA256:
`a21c8105910619e43c519942b39c26c3dab133d33929a9fe91994c05a5bc87eb`.

## SDK And Public Seams

Outer command logs, exits, outputs and full production inventories:
`/tmp/kotlin-ets-architecture-final-20260913-YaIdnI`

1. `node tools/kotlin-ets/tests/language/sdk.mjs`: PASS. Fresh CLI generation,
   actual SDK ETS inputs for LanguageSlice/DataSlice/ScopeSlice/ControlSlice/
   SingletonSlice, Ark bytecode and HAP; generated originals/staged bytes unchanged.
   Evidence `/private/tmp/kotlin-ets-language-sdk-7PmCju/manifest.json`.
   Signed HAP SHA256:
   `eeebd891e7260814bca288002f97a9b15e89ace2f6270e9a126427f173972b9a`.
2. `node tools/kotlin-ets/tests/stdlib/check-sdk.mjs`: PASS. Scalars/Lists both
   appear as ETS compiler inputs with nonempty protoBin, bytecode and HAP.
   Evidence `/tmp/kotlin-ets-stdlib-sdk-XiXngq/result.json`; integrity.json confirms
   unchanged read-only seed and generated bytes. Signed HAP SHA256:
   `7e1b3a55a716f618ef84d09b73e8e2ac5828b77121b7f9dc07e47c4c0550da8c`.
3. `bash tools/kotlin-ets/tests/target/run.sh`: PASS. Target tree/validator/printer
   compile and execute without Kotlin compiler dependencies on target classpaths.
   Evidence `kotlin-ets-target-tests.2NsnDo` under the system temporary directory;
   stdout is retained in the outer target/ command record.
4. `python3 -B tools/kotlin-ets/tests/integration/test_cli.py`: PASS3/3. Renamed
   methods/changed inputs execute; unknown platform call is source-linked with no
   output; unresolved frontend input emits no target. Runtime27.641s; full unittest
   report is outer `integration-cli/000-integration-cli.stderr`.

SDK hosts are isolated temporary clones. HAPs were built only, never installed.
The fixed SDK import shells are not page translation tests or device evidence.

## Frozen Identity

All15 production Kotlin-source/launcher identities remained unchanged throughout
the outer run. production-before.json and production-after.json have identical
SHA256 `e820d616b8cb571b5174144af2c79ccb96582ce60d46bd853ca52e1550345b8c`.
All nine production inputs shared with the backend run match that same baseline.

- LanguageLowering.kt: `58d64afbbda94f79131fa469d5e6692b86922e7bf4d635c379b9f4457ed9d5c5`
- StandardLibraryRules.kt: `9eaa26178766608f6c9d7bd410348f624a0d6b0133638a2ca87b4701cc18688f`
- ComposeEmitter.kt: `6bfa750b93b488e39b0f13dc685aeb4fdf101bd5998de17dff061d6520c76d3e`
- Core, target, support, launcher and remaining UI hashes are in the full inventory.

No production defect occurred in this final run. Historical RED logs remain as
documented in RESULTS.md; no failure was hidden or threshold relaxed. Verification
is limited to the declared first module subset, not full language compatibility,
page parity or device acceptance. All work has ended; no further writes or runs
are planned before the manager creates the whole-module review manifest.

## R2 Separate-Module Regression (2026-09-15)

The fixed R2 host queue exposed a later regression in the printer: smart-casting
`EtsUiElement.children` is invalid when the target tree is a separately compiled
module. The failed run is `kotlin-ets-backend-tests.5x6ABM` in the system temporary
directory. A local immutable binding fixes this without merging the modules or
changing the emitted UI structure. PrintContract now checks absent, empty and
nonempty children and verifies that the input node remains unchanged.

`bash tools/kotlin-ets/tests/backend/run.sh` passed in
`/var/folders/fj/rrz0bjhx6cq04j7yghy2qkxh0000gn/T/kotlin-ets-backend-tests.zoU7xF`.
The separate lowering/printer compilation, immutable repeated printing, and
original/renamed JVM-to-host differential all passed; input-sha256.txt was
rechecked against the worktree before publication. This is host/module evidence,
not SDK, native or whole-R2 acceptance.
